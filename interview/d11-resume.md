# D11 口述：从日志复活

程序一关，内存里的对话会丢，所以要把 D2 那本记录抄到文件。文件里存的就是原来那些事件，不是另一套存档格式。下次打开从文件读回来，再投影成给模型的信。若崩溃发生在工具结果已经记下、这一轮的结束还没记进账本，打开时补一条 turn/end（打断），已经写下的结果不要删。旁注：Claude Code 把 messages 当日记本再拿出来；dsh 存的是同一份事件流。
