# D13 口述：运行时改自己

聊天中途可以给自己加一个新按钮，比如查天气，下一封信的菜单里出现，循环不用重启；也能卸掉。这和 D1 一样是插件，只是从开机加载变成对话里再挂。D11 存的是对话记录，不是菜单上的临时按钮：已经查过天气的问答会回来，按钮默认不会还在。要每次开机都有，做成普通插件文件启动时加载。配套检查看账本合不合理，例如有 tool/call 必须有配对 tool/result，错了要响，不要默默跳过。旁注：Claude Code 主要是启动时装插件；dsh 还能在会话里当场挂上，且不把这种临时包当成对话事件去抄盘。
